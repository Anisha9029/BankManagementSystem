# BankManagementSystem
This Bank Management System is a Java-based application developed using layered architecture and JDBC. It allows users to perform CRUD operations such as creating, updating, deleting, and viewing bank accounts. The project ensures clean separation of concerns between model, DAO, services.
